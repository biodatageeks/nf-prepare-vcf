// Recompute every dosage even when the input file already carries one.
process {
    withName: 'CALC_DOSAGE_POLARSBIO' {
        ext.args = '--pl-type likelihood --ds-prior-min-af 0 --force-dosage-recalc'
    }
}
