// A named caller, which resolves --pl-type auto without the header being inspected at all.
process {
    withName: 'CALC_DOSAGE_POLARSBIO' {
        ext.args = '--variant-caller deepvariant --ds-prior-min-af 0'
    }
}
