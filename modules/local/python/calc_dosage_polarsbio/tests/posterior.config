// Flat prior, as used for DeepVariant-like input whose PL already holds posteriors.
process {
    withName: 'CALC_DOSAGE_POLARSBIO' {
        ext.args = '--pl-type posterior --ds-prior-min-af 0'
    }
}
